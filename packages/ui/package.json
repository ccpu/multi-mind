{
  "name": "@internal/ui",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The Multi Mind mark, plus the one import surface both windows take their components from",
  "exports": "./src/index.ts",
  "scripts": {
    "clean:all": "git clean -xdf .cache .turbo dist node_modules",
    "clean": "git clean -xdf .cache .turbo dist",
    "format": "prettier --check . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore",
    "format:fix": "prettier --write . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --run --coverage"
  },
  "peerDependencies": {
    "react": "catalog:prod"
  },
  "dependencies": {
    "@internal/utils": "workspace:*",
    "@pixpilot/shadcn": "catalog:prod",
    "@pixpilot/shadcn-ui": "catalog:prod",
    "tailwindcss": "catalog:prod"
  },
  "devDependencies": {
    "@internal/eslint-config": "workspace:*",
    "@internal/prettier-config": "workspace:*",
    "@internal/tsconfig": "workspace:*",
    "@internal/vitest-config": "workspace:*",
    "@testing-library/jest-dom": "catalog:dev",
    "@testing-library/react": "catalog:dev",
    "@testing-library/user-event": "catalog:dev",
    "@types/react": "catalog:dev",
    "@types/react-dom": "catalog:dev",
    "eslint": "catalog:dev",
    "prettier": "catalog:dev",
    "react": "catalog:prod",
    "react-dom": "catalog:prod",
    "typescript": "catalog:dev",
    "vitest": "catalog:dev"
  },
  "prettier": "@internal/prettier-config"
}
